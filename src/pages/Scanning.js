import { Button, Text, View, StyleSheet } from "react-native";

const ScanningPage = ({ navigation }) => {
  return (
    <View style={{ padding: 10, flex: 1, justifyContent: "space-evenly" }}>
      <Text>This is scanning
      </Text>
    </View>
  );
};


export default ScanningPage;
